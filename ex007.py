# crie um programa que calcule a media entre dois numeros fonecido pelo usuario

num1 = float(input('Digite o primeiro numero: '))
num2 = float(input('Digite o segundo numero: '))
med = (num1 + num2)/2
print('A media entre {} e {} é {} ' .format(num1, num2, med))
